spring:
  cloud:
    gateway:
      server:
        webflux:
          default-filters:
            - DedupeResponseHeader=X-Request-ID Access-Control-Allow-Origin Access-Control-Allow-Credentials, RETAIN_FIRST
          routes:
            - id: spring-java-api
              uri: "${GATEWAY_UPSTREAM_URI:lb://spring-java-service}"
              predicates:
                - Path=/api/**
              metadata:
                connect-timeout: 2000
                response-timeout: 10000
            - id: spring-java-health
              uri: "${GATEWAY_UPSTREAM_URI:lb://spring-java-service}"
              predicates:
                - Path=/management/spring-java/actuator/health,/management/spring-java/actuator/health/**
              filters:
                - StripPrefix=2
              metadata:
                connect-timeout: 2000
                response-timeout: 5000
          httpclient:
            connect-timeout: 2000
            response-timeout: 10s
          globalcors:
            add-to-simple-url-handler-mapping: true
            cors-configurations:
              '[/**]':
                allowed-origins:
                  - http://localhost:5173
                  - http://127.0.0.1:5173
                  - http://localhost:5174
                  - http://127.0.0.1:5174
                allowed-methods:
                  - GET
                  - POST
                  - PUT
                  - PATCH
                  - DELETE
                  - OPTIONS
                allowed-headers: '*'
                exposed-headers:
                  - X-Request-ID
                  - X-Service-Instance
                allow-credentials: true
                max-age: 3600

management:
  info:
    env:
      enabled: true

info:
  p1:
    config-source: nacos
    route-mode: load-balanced-dynamic-refresh
